#include <stdio.h>
#include <unistd.h>

int main(void)
{
    pid_t pid;

    pid = getpid();
    printf("Process ID is %d\n", pid);

    pid = getppid();
    printf("Parent Process ID is %d\n", pid);

    return 0;
}
